<Window x:Class="SplErpDC.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="SPL ERP Desktop Client"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        Height="720" Width="1280">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <ToolBar Grid.Row="0" Background="#f5f5f5">
            <Button Content="🏠 Home" Click="HomeButton_Click"/>
            <Button Content="⬅️ Back" Click="BackButton_Click"/>
            <Button Content="➡️ Forward" Click="ForwardButton_Click"/>
            <Button Content="🔄 Reload" Click="ReloadButton_Click"/>
            <Button Content="⛔ Stop" Click="StopButton_Click"/>
            <Separator/>
            <Button Content="➕ Zoom In" Click="ZoomIn_Click"/>
            <Button Content="➖ Zoom Out" Click="ZoomOut_Click"/>
            <Button Content="🔍 Reset Zoom" Click="ResetZoom_Click"/>
            <Separator/>
            <Button Content="🖨️ Print" Click="Print_Click"/>
            <Separator/>
            <Button Content="ℹ️ About" Click="AboutButton_Click"/>
            <Separator/>
            <Button Content="⚙️ Change Server" Click="ChangeUrlButton_Click"/>
            <Separator/>
            <Button Content="❌ Close" Click="CloseButton_Click"/>
            <ProgressBar x:Name="PageLoadingProgress" Width="200" Margin="5, 0, 0, 0" Height="16" IsIndeterminate="True" Visibility="Collapsed" VerticalAlignment="Center"/>
        </ToolBar>

        <!-- WebView and Loading -->
        <Grid Grid.Row="1">
            <wv2:WebView2 x:Name="webView" />
            <Grid x:Name="LoadingScreen" Background="#F0F0F0" Visibility="Visible">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="Loading ERP..." FontSize="24" Margin="0,0,0,10"/>
                    <ProgressBar IsIndeterminate="True" Width="200" Height="20"/>
                </StackPanel>
            </Grid>
            <ProgressBar Height="4" VerticalAlignment="Top" Visibility="Collapsed" IsIndeterminate="True" />
        </Grid>
    </Grid>
</Window>
